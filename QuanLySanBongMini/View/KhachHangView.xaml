<UserControl x:Class="QuanLySanBongMini.View.KhachHangView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLySanBongMini.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <SolidColorBrush x:Key="BeigeTextBrush" Color="#F5F5DC"/>
        <SolidColorBrush x:Key="DarkBeigeTextBrush" Color="#D4C5A3"/>
        <SolidColorBrush x:Key="MainGreenBrush" Color="#1E5631"/>
        <SolidColorBrush x:Key="ContentBeigeBrush" Color="#FAF8F5"/>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#DDE2E5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#1E5631"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#1E5631"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Width" Value="110"/>
            <Setter Property="Margin" Value="6,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.75"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#E0E0E0"/>
                                <Setter Property="Foreground" Value="#A0A0A0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="ForestGreen" Margin="16,16,16,12" Padding="4">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="1" Color="#000000" Opacity="0.1"/>
            </Border.Effect>
            <Border Background="ForestGreen" Padding="20,16">
                <StackPanel Orientation="Horizontal">
                    <Grid Width="4" Height="24" Background="#D4C5A3" Margin="0,0,12,0"/>
                    <TextBlock Text="QUẢN LÝ THÔNG TIN KHÁCH HÀNG" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" />
                </StackPanel>
            </Border>
        </Border>

        <Border Grid.Row="1" Background="#FFFFFF" Margin="16,0,16,12" CornerRadius="10" Padding="4">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2"
                      Color="#C8E6C9" Opacity="0.5"/>
            </Border.Effect>
            <Border Background="White" CornerRadius="7" Padding="20,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Column="0" Margin="8,4">
                        <TextBlock Text="Mã khách hàng" FontWeight="SemiBold" Foreground="#7A6F5D" Margin="0,0,0,8" FontSize="13"/>
                        <TextBox Text="{Binding MaKH, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="8,4">
                        <TextBlock Text="Tên khách hàng" FontWeight="SemiBold" Foreground="#7A6F5D" Margin="0,0,0,8" FontSize="13"/>
                        <TextBox Text="{Binding TenKH, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Margin="8,4">
                        <TextBlock Text="Số điện thoại" FontWeight="SemiBold" Foreground="#7A6F5D" Margin="0,0,0,8" FontSize="13"/>
                        <TextBox Text="{Binding SoDienThoai, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <WrapPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" HorizontalAlignment="Right" Margin="8,20,2,4">
                        <Button Command="{Binding ThemCommand}" Content="Thêm mới" Background="#10B981" Foreground="{StaticResource BeigeTextBrush}"/>
                        <Button Command="{Binding SuaCommand}" Content="Cập nhật" Background="#F59E0B" Foreground="{StaticResource BeigeTextBrush}"/>
                        <Button Command="{Binding XoaCommand}" Content="Xóa bỏ" Background="#EF4444" Foreground="{StaticResource BeigeTextBrush}"/>
                        <Button Command="{Binding LamMoiCommand}" Content="Làm trống" Background="#64748B" Foreground="{StaticResource BeigeTextBrush}"/>
                    </WrapPanel>
                </Grid>
            </Border>
        </Border>
        <Border Grid.Row="2" Background="#FFFFFF" Margin="16,0,16,16" CornerRadius="10" Padding="4">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2"
                      Color="#C8E6C9" Opacity="0.5"/>
            </Border.Effect>
            <Border Background="White" CornerRadius="7" Padding="20">
                <Grid>
                    <DataGrid AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column"
                              ItemsSource="{Binding DanhSachKhachHang}" SelectedItem="{Binding KhachHangDuocChon, Mode=TwoWay}"
                              Background="Transparent" RowHeight="40" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E2E8F0" BorderThickness="0"
                              Foreground="#4A402A" Margin="-13,-18,-13,-18">
                        <DataGrid.Resources >
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Padding" Value="12,10"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#E2E8F0"/>
                            </Style>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F7F4EB"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#EFEAE0"/>
                                        <Setter Property="Foreground" Value="#1E5631"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="12,0"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type DataGridCell}">
                                            <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" IsHitTestVisible="True">
                                                <ContentPresenter VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã khách hàng" Binding="{Binding MaKH}" Width="160"/>
                            <DataGridTextColumn Header="Họ và tên" Binding="{Binding TenKH}" Width="*"/>
                            <DataGridTextColumn Header="Số điện thoại" Binding="{Binding SoDienThoai}" Width="220"/>
                            <DataGridTextColumn Header="Số lần đặt sân" Binding="{Binding SoLanDatSan}" Width="160"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Border>
    </Grid>
</UserControl>
